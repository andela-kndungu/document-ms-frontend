(function() {
  'use strict';

  var should = require('should');
  describe('Documents', function() {
    describe('#indexOf(documents)', function () {
      it('should return -1 when the value is not present', function () {
        should(5).be.exactly(5).and.be.a.Number();
      });
    });
  });
})();
